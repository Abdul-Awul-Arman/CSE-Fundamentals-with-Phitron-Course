#include<stdio.h>
#include<math.h>
int main()
{
    // double a;
    // scanf("%lf",&a);
    // int ans=ceill(a);
    // int ans=floor(a);
    // int ans=round(a);
     
    // int a;
    // scanf("%d",&a);
    // double ans=sqrt(a); 
    // printf("%0.10lf",ans);

   //power int;
    // int a,b;
    // scanf("%d %d",&a,&b);

    // int ans=pow(a*1.0,b*1.0);
    // printf("%d",ans);

//power double;
    // double a,b;
    // scanf("%lf %lf",&a,&b);

    // double ans=pow(a,b);
    // printf("%lf",ans);
    
    
    int a;
    scanf("%d",&a);
    int ans=abs(a);
    printf("%d",ans);


    return 0;
}